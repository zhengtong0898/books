# 什么是原型模式(Prototype)?
原型是一种创造型模式, 以克隆形式创建一个对象. 克隆需要一个源对象(src)和一个目标对象(target)才能完成克隆操作.
对象的克隆主要描述的是将源对象的成员变量赋值到目标对象的成员变量中, 含关联对象的递归创建操作, 含继承对象的递归创建操作.

&nbsp;  

# 原型模式的处境
由于原型模式太过于常用, 以至于编程语言都必须将该模式内嵌到语言特性中.  
例如 python 中的 copy.deepcopy() 就能完整的克隆一个对象.   
例如 c++ 中的 复制构造函数 也能完整的克隆一个对象.

